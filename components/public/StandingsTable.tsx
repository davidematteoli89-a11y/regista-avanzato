import Link from "next/link";
import type { PublicStanding } from "@/lib/publicData/publicDataTypes";
export function StandingsTable({ standings }: { standings: readonly PublicStanding[] }) { if (!standings.length) return <p className="notice">Classifica non disponibile: copertura parziale nel dataset dimostrativo.</p>; return <div className="table-scroll"><table className="stats-table"><thead><tr><th>#</th><th>Squadra</th><th>G</th><th>V</th><th>N</th><th>P</th><th>GF</th><th>GS</th><th>DR</th><th>Pt</th></tr></thead><tbody>{standings.map((row) => <tr key={row.teamId}><td>{row.rank}</td><td><Link href={`/squadre/${row.teamId}`}>{row.teamName}</Link></td><td>{row.played}</td><td>{row.won}</td><td>{row.drawn}</td><td>{row.lost}</td><td>{row.goalsFor}</td><td>{row.goalsAgainst}</td><td>{row.goalDifference}</td><td><strong>{row.points}</strong></td></tr>)}</tbody></table></div>; }
