import type { VideoRadarItem } from "@/lib/videoRadar/videoRadarTypes";
import { EmbedAvailabilityBadge } from "./EmbedAvailabilityBadge";
export function VideoRadarCard({ item, preview = false }: { item: VideoRadarItem; preview?: boolean }) { return <article className={`video-radar-card${preview ? " preview-card" : ""}`}><div className="stats-badge-row"><span className="stats-badge">{item.type.replaceAll("_", " ")}</span><EmbedAvailabilityBadge availability={item.embedAvailability} /></div><h2>{item.title}</h2><p>{item.summary}</p><div className="search-result-meta">{item.tags.map((tag) => <span key={tag}>{tag}</span>)}</div>{item.originalContent && <p className="muted">Contenuto originale Regista Avanzato: script, commento e grafiche.</p>}{!preview && item.editorialNotes && <p className="video-editorial-note">Nota editoriale: {item.editorialNotes}</p>}{item.officialVideoUrl ? <a href={item.officialVideoUrl} target="_blank" rel="noreferrer">Apri la fonte ufficiale</a> : <p className="muted">Nessun URL ufficiale configurato: non viene mostrato alcun video.</p>}</article>; }
