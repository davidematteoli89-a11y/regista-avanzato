import type { CreatorPack } from "@/lib/videoScriptGenerator/videoScriptTypes";
const labels = { hook: "Hook", reel_seed: "Reel seed", short_script: "Script breve", carousel_idea: "Idea carosello", newsletter_idea: "Idea newsletter" } as const;
export function AdminCreatorPackPreview({ pack }: { pack: CreatorPack }) { return <section className="admin-section-card"><header><span className="admin-status">{pack.visibility}</span><h1>{pack.title}</h1><p>Ogni elemento resta non revisionato e non pubblicabile automaticamente.</p></header><div className="admin-log-list">{pack.items.map((item) => <article key={item.id}><div className="admin-card-head"><strong>{labels[item.type]} — {item.title}</strong><span className="admin-status">pending</span></div><p>{item.content}</p><p className="muted">Script collegato: {item.relatedScriptId ?? "nessuno"} · fonti: {item.sourceIds.length}</p></article>)}</div></section>; }
