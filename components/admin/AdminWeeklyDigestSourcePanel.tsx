import type { WeeklyDigestSource } from "@/lib/weeklyDigest/weeklyDigestTypes";
export function AdminWeeklyDigestSourcePanel({ sources }: { sources: readonly WeeklyDigestSource[] }) { return <section className="admin-section-card"><h2>Fonti settimanali</h2><div className="admin-log-list">{sources.map((source) => <article key={source.id}><div className="admin-card-head"><strong>{source.label}</strong><span className={`admin-status ${source.verified ? "status-approved" : "status-pending_review"}`}>{source.type}</span></div><p>{source.shortSummary}</p><dl className="admin-metadata"><dt>Snapshot weekly</dt><dd>{source.weeklySnapshot ? "Sì" : "No"}</dd><dt>Verificata</dt><dd>{source.verified ? "Sì" : "No"}</dd><dt>URL</dt><dd>{source.referenceUrl ?? "Nessun URL inventato"}</dd><dt>Testo lungo/video copiato</dt><dd>No / No</dd></dl></article>)}</div></section>; }
