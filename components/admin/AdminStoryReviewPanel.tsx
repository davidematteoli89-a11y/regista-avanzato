import type { StoryCopyrightCheckResult, StoryItem } from "@/lib/storyLibrary/storyTypes";
export function AdminStoryReviewPanel({ story, copyright }: { story: StoryItem; copyright: StoryCopyrightCheckResult }) { const warnings = [...story.reviewWarnings, ...copyright.warnings]; return <section className="admin-section-card"><h2>Review editoriale</h2><p>Stato: <strong>{story.reviewStatus}</strong></p>{warnings.length ? <ul>{warnings.map((warning) => <li key={warning}>{warning}</li>)}</ul> : <p>Nessun warning mock.</p>}<p className="notice">Pubblicazione automatica: vietata. È sempre richiesta una decisione umana.</p></section>; }
