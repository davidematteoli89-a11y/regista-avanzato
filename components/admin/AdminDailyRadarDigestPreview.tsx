import type { DailyRadarDigestPreview } from "@/lib/dailyRadar/dailyRadarTypes";
const Section = ({ title, items }: { title: string; items: readonly string[] }) => <section><h3>{title}</h3>{items.length ? <ul>{items.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="muted">Nessun elemento selezionato.</p>}</section>;
export function AdminDailyRadarDigestPreview({ digest }: { digest: DailyRadarDigestPreview }) { return <article className="admin-section-card daily-digest-preview"><span className="admin-status">{digest.visibility}</span><h2>{digest.title}</h2><p>{digest.opening}</p><Section title="Cosa guardare oggi" items={digest.watchToday} /><Section title="Idee articolo" items={digest.articleIdeas} /><Section title="Substack" items={digest.substackIdeas} /><Section title="Video / Reel" items={digest.videoIdeas} /><Section title="Semi Weekly Digest" items={digest.weeklyDigestSeeds} /><p className="notice">Preview interna: score tecnici inclusi pubblicamente? {digest.includesTechnicalScores ? "sì" : "no"}.</p></article>; }
