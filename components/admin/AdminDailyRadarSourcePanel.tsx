import type { DailyRadarSource } from "@/lib/dailyRadar/dailyRadarTypes";
export function AdminDailyRadarSourcePanel({ sources }: { sources: readonly DailyRadarSource[] }) { return <section className="admin-section-card"><h2>Fonti raccolte</h2><div className="admin-log-list">{sources.map((source) => <article key={source.id}><div className="admin-card-head"><strong>{source.label}</strong><span className={`admin-status ${source.verified ? "status-approved" : "status-pending_review"}`}>{source.type}</span></div><p>{source.shortSummary}</p><dl className="admin-metadata"><dt>Ufficiale</dt><dd>{source.official ? "Sì" : "No"}</dd><dt>Verificata</dt><dd>{source.verified ? "Sì" : "No"}</dd><dt>URL</dt><dd>{source.referenceUrl ?? "Nessun URL inventato"}</dd><dt>Testo lungo copiato</dt><dd>No</dd><dt>Video scaricato</dt><dd>No</dd></dl></article>)}</div></section>; }
