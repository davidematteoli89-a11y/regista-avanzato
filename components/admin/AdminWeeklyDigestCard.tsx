import Link from "next/link";
import type { WeeklyDigest } from "@/lib/weeklyDigest/weeklyDigestTypes";
export function AdminWeeklyDigestCard({ digest, overview }: { digest: WeeklyDigest; overview: { signalCount: number; sectionCount: number; candidateCount: number; urgentCount: number; highCount: number; suggestedDestinations: readonly string[] } }) { return <article className="admin-section-card"><div className="admin-card-head"><span className="admin-status">{digest.status}</span><span className="admin-status">{digest.visibility}</span></div><h2>{digest.title}</h2><p>{digest.weekStart} → {digest.weekEnd}</p><p>{overview.signalCount} segnali · {overview.sectionCount} sezioni · {overview.candidateCount} candidati</p><p className="muted">Urgent: {overview.urgentCount} · High: {overview.highCount} · destinazioni: {overview.suggestedDestinations.join(", ")}</p><Link href={`/admin/weekly-digest/${digest.id}`}>Apri digest privato</Link></article>; }
