import type { AdminTableColumn } from "@/lib/admin/adminTypes";
export function AdminTable<T extends object>({ columns, rows }: { columns: readonly AdminTableColumn[]; rows: readonly T[] }) { if (!rows.length) return <p className="admin-empty-inline">Nessun record mock disponibile.</p>; return <div className="table-scroll"><table className="stats-table admin-table"><thead><tr>{columns.map((column) => <th key={column.key}>{column.label}</th>)}</tr></thead><tbody>{rows.map((row, index) => { const record = row as Record<string, unknown>; return <tr key={String(record.id ?? index)}>{columns.map((column) => <td key={column.key}>{String(record[column.key] ?? "—")}</td>)}</tr>; })}</tbody></table></div>; }
