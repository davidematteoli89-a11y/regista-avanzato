import type { NewsletterDraftReviewItem } from "@/lib/newsletterGenerator/newsletterGeneratorTypes";

export function AdminNewsletterReviewChecklist({ items }: { items: readonly NewsletterDraftReviewItem[] }) { return <section className="admin-section-card"><h2>Checklist review umana</h2><ol className="article-review-list">{items.map((item) => <li key={item.id} className={`review-${item.status}`}><div><strong>{item.label}</strong><p>{item.description}</p></div><span className="admin-status">{item.status}</span></li>)}</ol><p className="notice">Tutti i 13 controlli sono obbligatori prima di copiare manualmente il contenuto verso una destinazione esterna.</p></section>; }
