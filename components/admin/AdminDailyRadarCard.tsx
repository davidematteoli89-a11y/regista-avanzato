import Link from "next/link";
import type { DailyRadarItem } from "@/lib/dailyRadar/dailyRadarTypes";
export function AdminDailyRadarCard({ item, overview }: { item: DailyRadarItem; overview: { signalCount: number; candidateCount: number; urgentCount: number; highCount: number; suggestedDestinations: readonly string[] } }) { return <article className="admin-section-card"><div className="admin-card-head"><span className="admin-status">{item.status}</span><span className="admin-status">{item.visibility}</span></div><h2>{item.title}</h2><p>{overview.signalCount} segnali · {overview.candidateCount} candidati · {overview.urgentCount} urgenti · {overview.highCount} high</p><p className="muted">Destinazioni: {overview.suggestedDestinations.join(", ")}</p><Link href={`/admin/daily-radar/${item.id}`}>Apri radar privato</Link></article>; }
