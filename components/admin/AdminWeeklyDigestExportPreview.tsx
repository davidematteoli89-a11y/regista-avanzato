import type { WeeklyDigestExportPreview } from "@/lib/weeklyDigest/weeklyDigestTypes";
export function AdminWeeklyDigestExportPreview({ preview }: { preview: WeeklyDigestExportPreview }) { return <section className="admin-section-card"><h2>Export preview in memoria</h2><div className="stats-badge-row"><span className="admin-status newsletter-plan-free">free {preview.freeSectionIds.length}</span><span className="admin-status newsletter-plan-paid">paid {preview.paidSectionIds.length}</span><span className="admin-status newsletter-plan-internal_only">internal {preview.internalSectionIds.length}</span></div><pre className="article-markdown-preview">{preview.markdown}</pre><p className="muted">Score tecnici: no · clip scaricate: no · file scritti: {preview.filesWritten}.</p></section>; }
