import type { DailyRadarSignal } from "@/lib/dailyRadar/dailyRadarTypes";
export function AdminDailyRadarSignalBox({ signal }: { signal: DailyRadarSignal }) { return <article className="admin-section-card"><div className="admin-card-head"><span className="admin-status">{signal.type}</span><span className={`admin-status ${signal.verifiedSources ? "status-approved" : "status-pending_review"}`}>{signal.verifiedSources ? "fonti disponibili" : "da verificare"}</span></div><h2>{signal.title}</h2><p>{signal.summary}</p><p className="muted">Fonti: {signal.sourceIds.length} · trigger forte: {signal.strongTrigger ? "sì" : "no"} · score non pubblico</p>{signal.warningCodes.length > 0 && <p className="notice">Warning: {signal.warningCodes.join(", ")}</p>}</article>; }
