import type { AdminBudgetUsage } from "@/lib/admin/adminTypes";
export function AdminBudgetBar({ usage }: { usage: AdminBudgetUsage }) { const percentage = usage.maximumEur > 0 ? Math.min(100, usage.currentEur / usage.maximumEur * 100) : 0; return <section className="admin-budget"><div className="admin-card-head"><strong>{usage.label}</strong><span>{usage.currentEur.toFixed(2)} € / {usage.maximumEur.toFixed(2)} €</span></div><div className="budget-track" aria-label={`Budget usato ${percentage.toFixed(0)}%`}><span style={{ width: `${percentage}%` }} /></div><div className="admin-budget-meta"><span>Warning {usage.warningEur} €</span><span>Hard stop {usage.hardStopEur} €</span><span>Residuo mock {usage.remainingEur} €</span></div></section>; }
