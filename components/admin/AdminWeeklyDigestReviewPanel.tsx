import type { WeeklyDigestCandidate } from "@/lib/weeklyDigest/weeklyDigestTypes";
import { AdminWeeklyDigestDestinationBadge } from "./AdminWeeklyDigestDestinationBadge";
import { AdminWeeklyDigestPriorityBadge } from "./AdminWeeklyDigestPriorityBadge";
export function AdminWeeklyDigestReviewPanel({ candidate }: { candidate: WeeklyDigestCandidate }) { return <article id={candidate.id} className={`admin-section-card weekly-candidate review-${candidate.reviewStatus}`}><div className="admin-card-head"><AdminWeeklyDigestPriorityBadge priority={candidate.priority} /><span className="admin-status">{candidate.reviewStatus}</span></div><h2>{candidate.title}</h2><p>{candidate.whyInteresting}</p><div className="stats-badge-row">{candidate.destinations.map((destination) => <AdminWeeklyDigestDestinationBadge key={destination} destination={destination} />)}</div><dl className="admin-metadata"><dt>Score interno</dt><dd>{candidate.score.value}/100</dd><dt>Fonti</dt><dd>{candidate.sourceIds.length}</dd><dt>Review umana</dt><dd>Obbligatoria</dd><dt>Auto publish/send/produce</dt><dd>No / No / No</dd></dl>{candidate.warnings.length > 0 && <p className="admin-warning critical">{candidate.warnings.join(" ")}</p>}</article>; }
