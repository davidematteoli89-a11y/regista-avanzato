import type { StoryImportPreview } from "@/lib/storyLibrary/storyTypes";
export function AdminStoryImportPreview({ preview }: { preview: StoryImportPreview }) { return <section className="admin-section-card"><h2>Preview import Markdown</h2><p>Stato: <strong>{preview.status}</strong> · caratteri: {preview.bodyCharacterCount}</p><pre className="story-frontmatter-preview">{JSON.stringify(preview.frontmatter, null, 2)}</pre><p>{preview.bodyPreview}</p>{preview.errors.length > 0 && <div className="admin-warning critical"><strong>Errori</strong><ul>{preview.errors.map((error) => <li key={error}>{error}</li>)}</ul></div>}{preview.warnings.length > 0 && <div className="admin-warning"><strong>Warning</strong><ul>{preview.warnings.map((warning) => <li key={warning}>{warning}</li>)}</ul></div>}<p className="muted">File letti: 0 · file scritti: 0 · database scritto: 0 · auto-publish: no.</p></section>; }
