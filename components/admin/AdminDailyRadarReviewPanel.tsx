import type { DailyRadarCandidate } from "@/lib/dailyRadar/dailyRadarTypes";
import { AdminDailyRadarDestinationBadge } from "./AdminDailyRadarDestinationBadge";
import { AdminDailyRadarPriorityBadge } from "./AdminDailyRadarPriorityBadge";
export function AdminDailyRadarReviewPanel({ candidate }: { candidate: DailyRadarCandidate }) { return <article id={candidate.id} className={`admin-section-card daily-candidate review-${candidate.reviewStatus}`}><div className="admin-card-head"><AdminDailyRadarPriorityBadge priority={candidate.priority} /><span className="admin-status">{candidate.reviewStatus}</span></div><h2>{candidate.title}</h2><p>{candidate.whyInteresting}</p><div className="stats-badge-row">{candidate.destinations.map((destination) => <AdminDailyRadarDestinationBadge key={destination} destination={destination} />)}</div><dl className="admin-metadata"><dt>Score interno</dt><dd>{candidate.score.value}/100</dd><dt>Fonti</dt><dd>{candidate.sourceIds.length}</dd><dt>Review umana</dt><dd>Obbligatoria</dd><dt>Auto publish/send/produce</dt><dd>No / No / No</dd></dl>{candidate.warnings.length > 0 && <p className="admin-warning critical">{candidate.warnings.join(" ")}</p>}</article>; }
