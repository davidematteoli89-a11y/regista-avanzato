import type { WeeklyDigestSection } from "@/lib/weeklyDigest/weeklyDigestTypes";
export function AdminWeeklyDigestSectionBox({ section }: { section: WeeklyDigestSection }) { return <section className={`admin-section-card weekly-section weekly-${section.visibility}`}><div className="admin-card-head"><span className="admin-status">{section.type}</span><span className="admin-status">{section.visibility}</span></div><h2>{section.title}</h2><p>{section.description}</p>{section.items.length ? <ul>{section.items.map((item) => <li key={item.id}><strong>{item.title}</strong><span>{item.summary}</span><small>{item.factLabel} · fonti {item.sourceIds.length} · URL {item.officialUrl ?? "non disponibile"}</small></li>)}</ul> : <p className="muted">Nessun elemento verificato disponibile.</p>}</section>; }
