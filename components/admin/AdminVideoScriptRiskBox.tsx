import type { VideoScriptRisk, VideoScriptRiskLevel } from "@/lib/videoScriptGenerator/videoScriptTypes";
export function AdminVideoScriptRiskBox({ riskLevel, risks }: { riskLevel: VideoScriptRiskLevel; risks: readonly VideoScriptRisk[] }) { return <section className={`admin-warning article-risk risk-${riskLevel}`}><strong>Rischio complessivo: {riskLevel}</strong>{risks.length ? <ul>{risks.map((risk) => <li key={risk.id}><strong>{risk.code}</strong>: {risk.message}</li>)}</ul> : <p>Nessun rischio specifico rilevato dal mock; review comunque obbligatoria.</p>}<p>Generazione video: vietata · produzione automatica: vietata · pubblicazione automatica: vietata.</p></section>; }
