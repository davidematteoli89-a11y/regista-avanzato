import type { VideoScriptDraft } from "@/lib/videoScriptGenerator/videoScriptTypes";
import { AdminVideoFormatBadge } from "./AdminVideoFormatBadge";
export function AdminVideoScriptPreview({ script }: { script: VideoScriptDraft }) { return <article className="admin-section-card video-script-preview"><header><div className="stats-badge-row"><AdminVideoFormatBadge format={script.format} /><span className="admin-status">{script.destination}</span><span className="admin-status">{script.visibility}</span></div><h1>{script.title}</h1><p><strong>Hook:</strong> {script.hook.text}</p></header>{script.sections.map((section) => <section key={section.id}><div className="admin-card-head"><h2>Scena {section.scene}</h2><span className="admin-status">{section.timing}</span></div><h3>Voiceover</h3><p>{section.voiceover}</p><h3>Testo on screen</h3><p>{section.onScreenText}</p><h3>Visual</h3><ul>{section.visualCues.map((cue) => <li key={cue.id}><strong>{cue.kind}</strong>: {cue.description} {cue.rightsReviewRequired && "— verifica diritti"}</li>)}</ul><p className="muted">Confidenza: {section.factConfidence} · operazioni video vietate richieste: 0</p></section>)}<section><h2>CTA</h2><p>{script.cta.text}</p></section></article>; }
