import type { NewsletterDraft } from "@/lib/newsletterGenerator/newsletterGeneratorTypes";
import { AdminNewsletterFormatBadge } from "./AdminNewsletterFormatBadge";
import { AdminSubstackPlanBadge } from "./AdminSubstackPlanBadge";

export function AdminNewsletterDraftPreview({ draft }: { draft: NewsletterDraft }) { return <article className="admin-section-card newsletter-draft-preview"><header><div className="stats-badge-row"><AdminNewsletterFormatBadge format={draft.format} /><AdminSubstackPlanBadge plan={draft.plan} /><span className="admin-status">{draft.visibility}</span></div><h1>{draft.title}</h1><p><strong>Oggetto:</strong> {draft.subject}</p><p>{draft.preheader}</p></header>{draft.sections.map((section) => <section key={section.id} className={section.paidOnly ? "newsletter-paid-section" : undefined}><div className="admin-card-head"><h2>{section.title}</h2>{section.paidOnly && <span className="admin-status newsletter-plan-paid">paid</span>}</div><p className="muted">{section.purpose}</p><ul>{section.items.map((item, index) => <li key={`${section.id}-${index}`}>{item}</li>)}</ul><p className="muted">Confidenza: {section.factConfidence} · fonti: {section.sourceIds.length}</p></section>)}</article>; }
