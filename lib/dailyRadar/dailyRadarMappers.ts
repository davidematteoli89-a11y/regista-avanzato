import { evaluateDailyRadarSignal } from "./dailyRadarRules";
import { calculateDailyRadarScore, priorityFromScore } from "./dailyRadarScoring";
import type { DailyRadarCandidate, DailyRadarCandidateType, DailyRadarDestination, DailyRadarSignal } from "./dailyRadarTypes";
const candidateType = (type: DailyRadarSignal["type"]): DailyRadarCandidateType => type === "historical_echo" ? "historical_echo_candidate" : type === "young_player" || type === "talent_signal" ? "talent_candidate" : type === "crazy_match" || type === "high_scoring_match" || type === "late_goal" || type === "comeback" ? "match_candidate" : type === "official_news" || type === "rumor_to_review" ? "news_candidate" : type === "video_watch" ? "video_candidate" : type === "reel_seed" || type === "creator_pack_seed" ? "reel_candidate" : type === "newsletter_seed" ? "substack_candidate" : "article_candidate";
const destinations = (type: DailyRadarSignal["type"]): DailyRadarDestination[] => type === "historical_echo" ? ["website_article", "weekly_digest", "reel_script"] : type === "video_watch" ? ["video_radar", "reel_script"] : type === "newsletter_seed" ? ["substack_free", "substack_paid", "weekly_digest"] : type === "reel_seed" ? ["reel_script", "creator_pack"] : type === "creator_pack_seed" ? ["creator_pack"] : type === "rumor_to_review" ? ["admin_review", "private_note"] : type === "crazy_match" || type === "high_scoring_match" ? ["website_radar", "website_article", "weekly_digest"] : type === "young_player" || type === "talent_signal" ? ["website_radar", "substack_paid", "video_radar"] : ["website_article", "weekly_digest"];
export function mapSignalToCandidate(signal: DailyRadarSignal): DailyRadarCandidate { const score = calculateDailyRadarScore(signal); const rules = evaluateDailyRadarSignal(signal); return { id: `daily-candidate-${signal.id}`, type: candidateType(signal.type), signalId: signal.id, title: signal.title, whyInteresting: rules.blocked ? "Segnale da verificare e mantenere privato." : `Segnale ${signal.type.replace(/_/g, " ")} con fonti mock disponibili; valutare contesto e originalità editoriale.`, status: rules.blocked ? "pending_review" : "candidate", visibility: "private_admin", priority: priorityFromScore(score.value), score, destinations: destinations(signal.type), sourceIds: [...signal.sourceIds], warnings: [...rules.warnings], reviewStatus: rules.blocked ? "blocked" : "not_reviewed", requiresHumanReview: true, autoPublish: false, autoSend: false, autoProduce: false }; }
