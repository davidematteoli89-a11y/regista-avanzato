import { MOCK_WEEKLY_DIGESTS } from "./mockWeeklyDigestData";
export async function getAdminWeeklyDigests() { return MOCK_WEEKLY_DIGESTS.map((digest) => ({ digest, overview: { signalCount: digest.signals.length, sectionCount: digest.sections.length, candidateCount: digest.candidates.length, urgentCount: digest.candidates.filter((candidate) => candidate.priority === "urgent").length, highCount: digest.candidates.filter((candidate) => candidate.priority === "high").length, suggestedDestinations: [...new Set(digest.candidates.flatMap((candidate) => candidate.destinations))] } })); }
