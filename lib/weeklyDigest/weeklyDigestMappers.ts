import { evaluateWeeklyDigestSignal } from "./weeklyDigestRules";
import { calculateWeeklyDigestScore, weeklyPriorityFromScore } from "./weeklyDigestScoring";
import type { WeeklyDigestCandidate, WeeklyDigestCandidateType, WeeklyDigestDestination, WeeklyDigestSignal, WeeklyDigestSource } from "./weeklyDigestTypes";
const typeFor = (type: WeeklyDigestSignal["type"]): WeeklyDigestCandidateType => type === "apify_weekly_signal" || type === "minor_league_signal" ? "minor_league_report_candidate" : type === "historical_echo" ? "historical_echo_candidate" : type === "young_player" || type === "talent_signal" || type === "stats_spike" ? "talent_candidate" : type === "crazy_match" || type === "high_scoring_match" || type === "late_goal" || type === "comeback" ? "match_candidate" : type === "official_news" || type === "rumor_to_review" ? "news_candidate" : type === "video_watch" ? "video_candidate" : type === "reel_seed" ? "reel_candidate" : type === "creator_pack_seed" ? "creator_pack_candidate" : type === "newsletter_seed" ? "substack_free_candidate" : "article_candidate";
const destinationsFor = (type: WeeklyDigestSignal["type"]): WeeklyDigestDestination[] => type === "apify_weekly_signal" || type === "minor_league_signal" ? ["substack_paid", "private_note"] : type === "historical_echo" ? ["website_article", "reel_script"] : type === "video_watch" ? ["video_radar", "reel_script"] : type === "newsletter_seed" ? ["substack_free", "substack_paid"] : type === "reel_seed" ? ["reel_script", "creator_pack"] : type === "creator_pack_seed" ? ["creator_pack"] : type === "rumor_to_review" ? ["admin_review", "private_note"] : type === "crazy_match" ? ["website_radar", "website_article"] : type === "young_player" || type === "talent_signal" || type === "stats_spike" ? ["website_radar", "substack_paid", "video_radar"] : ["website_article"];
export function mapWeeklySignalToCandidate(signal: WeeklyDigestSignal, sources: readonly WeeklyDigestSource[]): WeeklyDigestCandidate { const score = calculateWeeklyDigestScore(signal); const rules = evaluateWeeklyDigestSignal(signal, sources); return { id: `weekly-candidate-${signal.id}`, type: typeFor(signal.type), signalId: signal.id, title: signal.title, whyInteresting: rules.blocked ? "Segnale da mantenere privato e verificare." : `Segnale settimanale ${signal.type.replace(/_/g, " ")} da contestualizzare e revisionare.`, status: rules.blocked ? "pending_review" : "candidate", visibility: "private_admin", priority: weeklyPriorityFromScore(score.value), score, destinations: destinationsFor(signal.type), sourceIds: [...signal.sourceIds], warnings: rules.warnings, reviewStatus: rules.blocked ? "blocked" : "not_reviewed", requiresHumanReview: true, autoPublish: false, autoSend: false, autoProduce: false }; }
