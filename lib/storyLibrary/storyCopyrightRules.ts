import type { StoryCopyrightCheckResult, StorySourceType } from "./storyTypes";
export const MAX_STORY_IMPORT_CHARACTERS = 4_000;
export const MAX_EXTERNAL_SUMMARY_CHARACTERS = 1_200;
const THIRD_PARTY: readonly StorySourceType[] = ["markdown_file", "pdf_reference", "book_reference", "article_reference", "video_reference", "official_source"];
export function checkStoryCopyright(input: { sourceType?: StorySourceType; body: string; sourceReference?: string; claimsOriginal?: boolean }): StoryCopyrightCheckResult { const warnings: string[] = []; const sourceType = input.sourceType; const external = sourceType ? THIRD_PARTY.includes(sourceType) : true; if (input.body.length > MAX_STORY_IMPORT_CHARACTERS) warnings.push("Contenuto troppo lungo per un import editoriale sicuro."); if (external && !input.sourceReference?.trim()) warnings.push("Fonte obbligatoria per materiale derivato da terzi."); if (external && input.body.length > MAX_EXTERNAL_SUMMARY_CHARACTERS) warnings.push("Materiale da terzi troppo esteso: conservare soltanto riassunto breve e note originali."); if (external) warnings.push("PDF, libri, articoli, video e Markdown esterni richiedono review umana."); if (!input.claimsOriginal && sourceType === "original_note") warnings.push("La nota dichiarata originale deve essere confermata dall'autore."); const blocked = input.body.length > MAX_STORY_IMPORT_CHARACTERS || (external && !input.sourceReference?.trim()) || (external && input.body.length > MAX_EXTERNAL_SUMMARY_CHARACTERS); return { allowed: !blocked, autoPublishAllowed: false, requiresHumanReview: external || warnings.length > 0, blocked, warnings, reason: blocked ? "Import bloccato per lunghezza o provenienza insufficiente." : "Anteprima consentita; pubblicazione automatica sempre vietata." }; }
