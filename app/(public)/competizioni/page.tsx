import { CompetitionCard } from "@/components/public/CompetitionCard";
import { PublicStatFilters } from "@/components/public/PublicStatFilters";
import { getPublicCompetitions } from "@/lib/publicData/getPublicCompetitions";
export default async function CompetitionsPage() { const data = await getPublicCompetitions(); return <main className="stack"><header><span className="eyebrow">Public Stats Hub</span><h1>Competizioni</h1><p>Campionati, risultati e copertura pubblica da snapshot interni. In questo step tutti i dati sono dimostrativi.</p></header><PublicStatFilters /><div className="public-stats-grid">{data.items.map((competition) => <CompetitionCard key={competition.id} competition={competition} />)}</div><p className="notice">{data.meta.warning} Navigare nello Stats Hub non consuma ricerche avanzate.</p></main>; }
