import { AdminSectionCard } from "@/components/admin/AdminSectionCard";
import { AdminStatCard } from "@/components/admin/AdminStatCard";
import { AdminWarningBox } from "@/components/admin/AdminWarningBox";
import { getAdminOverview } from "@/lib/admin/getAdminOverview";
export default async function AdminOverviewPage() { const overview = await getAdminOverview(); return <main className="admin-page"><header><h2>Overview progetto</h2><p>Stato tecnico-editoriale mock. Step completati: {overview.completedSteps}.</p></header><div className="admin-stats-grid">{overview.stats.map((stat) => <AdminStatCard key={stat.id} stat={stat} />)}</div><div className="admin-warning-grid">{overview.warnings.map((warning) => <AdminWarningBox key={warning.id} warning={warning} />)}</div><div className="admin-section-grid"><AdminSectionCard title="Dati e import" description="Competizioni, provider, budget e pipeline dry-run." href="/admin/imports" /><AdminSectionCard title="Radar editoriali" description="Candidati privati e queue soggette a review." href="/admin/content-radar" /><AdminSectionCard title="Utenti free" description="Profili e quota ricerca soltanto mock." href="/admin/users" /></div></main>; }
